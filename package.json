{
  "name": "sunup",
  "private": true,
  "version": "0.1.0",
  "description": "Per-worker heat acclimatization state and heat work planning with live FortyGuard site weather.",
  "scripts": {
    "check:design": "node scripts/check-design.mjs",
    "prebuild": "npm run check:design",
    "build": "python scripts/build_roster_data.py"
  }
}
