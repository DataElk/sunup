"""Physical models. No I/O, no fixtures, no FortyGuard — pure functions of numbers.

Everything in here is testable against a textbook or a hand calculation, which is
the point: if the WBGT pipeline is wrong, the bug is findable without a network.
"""
